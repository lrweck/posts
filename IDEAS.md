# Ideas (backlog)

Ideas for later. Not planned, not in progress.

1. **Comments with Giscus** — GitHub has públic already; comments backend via GH Discussions. ~10 min to set up.
2. **Avatar + bio on homepage** — layout `profile` is active; missing avatar image and bio text.
3. **About page** — single file, add to menu. Gives the blog personality.
4. **OG image / social card** — show post cover when shared on Twitter/WhatsApp. `defaultSocialImage` path already referenced (commented) in `params.toml`.
5. **Private analytics** — GoatCounter/Umami, few lines in `params.toml`.

## Not viable on GitHub Pages
- Views/likes (Blowfish uses Firebase, needs dynamic backend).